//Header with footerTitle and footerDescription
export default class Footer {
    constructor(footerTitle) {
        this.footerTitle = footerTitle;
   }
}

export const footerContent = new Footer('For lunch');
//  Variable with new footer
